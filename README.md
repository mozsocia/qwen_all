# qwen_all

tell qwen to check error by first run `npx tsc --noEmit` then `npm run lint` and do not run build or dev

```
- to check error first run `npx tsc --noEmit` then `npm run lint`
- do not run `npm run dev` because it is already running in backgroud
- do not run `npm run build` also

```
